Examples of code from prior tasks over the years. 


AvayaIfx.cs - 

CleanPC-Fast.bat -

CleanPC.ps1 - 

De-Excel.htm - Removes annoying Excel bits and acts as a paste pad.

README.md - this file

add-printers.ps1 - Adds printer using UNC to workstation remotely.

create_redis.sql - table and view to emulate Redis in MSSQL after Redis for Windows became more difficult to acquire.

del-printers.ps1 - Deletes printer using UNC from workstation remotely.

ics-file.htm - Needs javascript libraries, but shows link, usually in Excel, to ICS for Outlook. 

last-boot.bat - Started as literally last boot of Windows device, but evolved into more useful info.

last-boot.ps1 - Attempt to rewrite last-boot.bat as pure PowerShell.

make.bat - Example of compiling C# at the command line. Useful before I was allowed an IDE at work after SharpDevelop was disallowed. 

show.dcl - Basically, top for job queues when I was a computer operator. Occasionally, showed 1% CPU use and the admins were not pleased.

wiegand.ps1 - A certain printer company, that also makes cameras, has issues with reading only 16 bits out of the 18 bit # on a card. This causes all sorts of fun collisions with users. 
